version: '3'
services:
  ws:
    build: web_services
    depends_on:
      - db
    ports:
      - "3000:3000"
      - "80:80"
    network_mode: host

  anti_sybil:
    build: anti_sybil
    depends_on:
      - ws
      - db
    network_mode: host

  db:
    build: db_dumps
    network_mode: host
    ports:
      - "8529:8529"
